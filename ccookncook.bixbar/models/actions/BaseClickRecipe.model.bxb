action (BaseClickRecipe) {
  type (Search)
  description (base list 에서 해당 칵테일을 클릭했을 때)
  collect {
    input (title) {
      type (Title)
      min (Required) max (One)
    } 
  }
  output (BaseClickResults)
}
