const gender = ["Female", "Male"];

module.exports.function = function findGender() {
  //if (!searchTerm) {
    return gender;
  //}

  //return gender.filter(gender => {
  //  return gender[0].toLowerCase().includes(searchTerm) || gender[1].toLowerCase().includes(searchTerm)
  //});
}
