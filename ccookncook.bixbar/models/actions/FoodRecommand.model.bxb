action (FoodRecommand){
  type(Search)

  description (recommand food action)
  collect {
    input (queryInput) {
      type (QueryInput)
      min (Required) max (One)
    }
  }
  output (FoodResults)
}
